// Backup of original translations before fixing syntax errors
export const translations = {
  // This is a backup file created during syntax error fixes
  // The main translations.js file will be cleaned up
};
